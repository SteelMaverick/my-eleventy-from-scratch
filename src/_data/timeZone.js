/*import Fetch from "@11ty/eleventy-fetch";

export default async () => {
    //let query = '';
    let url = "http://ip-api.com/json/?fields=57600"; //`http://ip-api.com/json/${query}?fields=45146399`;
	try {
		const json = await Fetch(url, {
			duration: '30s',
			type: 'json',
		});
        return json;
	} catch (err) {
		console.log(err);
        return [];
	}
};*/
